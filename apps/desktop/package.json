{
  "name": "@hhousing/desktop",
  "version": "0.1.0",
  "private": true,
  "main": "dist/main.js",
  "scripts": {
    "build:main": "tsc -p tsconfig.main.json",
    "dev": "pnpm build:main && electron .",
    "typecheck": "tsc -p tsconfig.main.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.14.0",
    "electron": "^35.7.5",
    "typescript": "~5.8.3"
  }
}
