# desktop

Isolated Electron desktop shell for Hhousing. This package is separate from the Expo mobile app (`apps/mobile-tenant`) and the web apps.

## Development

From the repository root:

```bash
pnpm desktop:dev
```

Or from this directory:

```bash
pnpm dev
```
