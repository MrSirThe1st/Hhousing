import UniversalLoadingState from "../../components/universal-loading-state";

export default function LoginLoading(): React.ReactElement {
  return <UniversalLoadingState minHeightClassName="min-h-screen" />;
}
